name: Infrastructure - Provision Azure Resources

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - dev
          - staging
          - production

permissions:
  id-token: write
  contents: read

jobs:
  provision:
    name: Provision Azure Infrastructure
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Bicep Infrastructure
        id: deploy
        run: |
          DEPLOYMENT_NAME="petclinic-${{ github.event.inputs.environment }}-${{ github.run_id }}"
          echo "DEPLOYMENT_NAME=$DEPLOYMENT_NAME" >> "$GITHUB_OUTPUT"
          az deployment sub create \
            --name "$DEPLOYMENT_NAME" \
            --location ${{ vars.AZURE_LOCATION }} \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.json \
            --parameters environmentName="${{ github.event.inputs.environment }}-petclinic"

      - name: Show deployment outputs
        run: |
          az deployment sub show \
            --name "${{ steps.deploy.outputs.DEPLOYMENT_NAME }}" \
            --query properties.outputs
