# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: spring-petclinic
metadata:
  template: spring-petclinic@0.0.1-beta

services:
  web:
    project: .
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
